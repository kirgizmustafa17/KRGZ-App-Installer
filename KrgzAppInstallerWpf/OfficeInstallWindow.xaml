<Window x:Class="KrgzAppInstaller.OfficeInstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Microsoft Office Kurulumu (ODT)" Width="520" Height="600"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#0D1117" Foreground="#C9D1D9" FontFamily="Segoe UI">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Microsoft Office Bileşenleri" FontSize="18" FontWeight="Bold" Foreground="#F0F6FC" Margin="0,0,0,6"/>
        <TextBlock Grid.Row="1" Text="Kurmak istediğiniz Office uygulamalarını seçin:" Foreground="#8B949E" Margin="0,0,0,14" FontSize="13"/>

        <Border Grid.Row="2" Background="#161B22" CornerRadius="8" Padding="16" BorderBrush="#30363D" BorderThickness="1">
            <StackPanel x:Name="pnlOfficeApps"/>
        </Border>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="Kanal:" Foreground="#58A6FF" Margin="0,0,0,4" FontSize="12" FontWeight="SemiBold"/>
                <ComboBox x:Name="cmbChannel" Background="#E6EDF3" Foreground="#0D1117" FontSize="13" Padding="6,4" SelectedIndex="0">
                    <ComboBoxItem Content="Current Channel"/>
                    <ComboBoxItem Content="Monthly Enterprise"/>
                    <ComboBoxItem Content="Semi-Annual Enterprise"/>
                </ComboBox>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock Text="Mimari:" Foreground="#58A6FF" Margin="0,0,0,4" FontSize="12" FontWeight="SemiBold"/>
                <ComboBox x:Name="cmbArch" Background="#E6EDF3" Foreground="#0D1117" FontSize="13" Padding="6,4" SelectedIndex="0">
                    <ComboBoxItem Content="64-bit"/>
                    <ComboBoxItem Content="32-bit"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="4" x:Name="txtStatus" Text="" Foreground="#E3B341" Margin="0,10,0,0" FontSize="12" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="btnCancel" Content="İptal" Width="90" Height="34" Margin="0,0,10,0" 
                    Background="#21262D" Foreground="#C9D1D9" BorderBrush="#30363D" FontSize="13" Cursor="Hand"
                    Click="BtnCancel_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="6,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="btnInstall" Content="Kur" Width="120" Height="34" 
                    Background="#238636" Foreground="White" BorderBrush="#2EA043" FontSize="14" FontWeight="SemiBold" Cursor="Hand"
                    Click="BtnInstall_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="6,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
